apiVersion: v1
kind: Service
metadata:
  name: {{ include "budget-buddy.fullname" . }}
  labels:
    {{- include "budget-buddy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "budget-buddy.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: app
